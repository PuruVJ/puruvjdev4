{
	"name": "worker-typescript-template",
	"version": "1.0.0",
	"description": "Cloudflare worker TypeScript template",
	"main": "dist/worker.js",
	"scripts": {
		"build": "esbuild src/index.ts --bundle --minify --platform=node --target=es2020,chrome90 --outfile=dist/worker.js"
	},
	"author": "author",
	"license": "MIT OR Apache-2.0",
	"devDependencies": {
		"@cloudflare/workers-types": "^3.2.0",
		"esbuild": "^0.13.14",
		"typescript": "^4.5.2"
	},
	"dependencies": {
		"worktop": "^0.7.3"
	}
}
